spring.application.name=complaintmanagement

# Database - use environment variables on Render
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/hcms}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=${PORT:8080}

# Profile image uploads (relative to working directory)
file.upload-dir=uploads

# Allow larger image uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Google Maps Geocoding
google.maps.api-key=${GOOGLE_MAPS_API_KEY:}

# Mail Configuration (Gmail) - use environment variables in production
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${MAIL_USERNAME:badshot62@gmail.com}
spring.mail.password=${MAIL_PASSWORD:doyr dmky uelr pqwl}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.starttls.enable=false
