spring.application.name=complaintmanagement

spring.datasource.url=jdbc:mysql://localhost:3306/hcms
spring.datasource.username=root
spring.datasource.password=akshat123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

server.port=${PORT:8080}

# Profile image uploads (relative to working directory)
file.upload-dir=uploads

# Allow larger image uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Google Maps Geocoding (server-side; do NOT commit real keys)
# Set via environment variable in production, e.g. GOOGLE_MAPS_API_KEY=...
google.maps.api-key=${GOOGLE_MAPS_API_KEY:}

spring.web.resources.static-locations=file:C:/Users/Akshat/Desktop/hcms1/frontend/

# Mail Configuration (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=badshot62@gmail.com
spring.mail.password=doyr dmky uelr pqwl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
